<UserControl x:Class="CalculatorMateriale.Views.RapoarteView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Background="{StaticResource CanvasBrush}" Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,14">
            <TextBlock Text="Rapoarte vânzări" Style="{StaticResource TitleTextBlockStyle}"/>
            <TextBlock Text="Comenzi, clienți, materiale vândute și export Excel." Style="{StaticResource SubtitleTextBlockStyle}"/>
        </StackPanel>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="110"/>
                    <ColumnDefinition Width="110"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="De la" FontSize="11" FontWeight="Bold"/>
                    <DatePicker x:Name="StartDatePicker" Height="34" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="Până la" FontSize="11" FontWeight="Bold"/>
                    <DatePicker x:Name="EndDatePicker" Height="34" Margin="0,5,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="0,0,10,0">
                    <TextBlock Text="Status" FontSize="11" FontWeight="Bold"/>
                    <ComboBox x:Name="StatusCombo" Height="34" Margin="0,5,0,0">
                        <ComboBoxItem Content="Toate" IsSelected="True"/>
                        <ComboBoxItem Content="Noua"/>
                        <ComboBoxItem Content="Confirmata"/>
                        <ComboBoxItem Content="Finalizata"/>
                        <ComboBoxItem Content="Anulata"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="0,0,10,0">
                    <TextBlock Text="Caută client/material" FontSize="11" FontWeight="Bold"/>
                    <TextBox x:Name="SearchBox" Height="34" Margin="0,5,0,0" Style="{StaticResource DefaultTextBoxStyle}"/>
                </StackPanel>

                <Button Grid.Column="4" Content="Filtrează" Height="34" Margin="0,19,8,0"
                        Style="{StaticResource PrimaryButtonStyle}" Click="FilterButton_Click"/>
                <Button Grid.Column="5" Content="Excel" Height="34" Margin="0,19,0,0"
                        Style="{StaticResource SecondaryButtonStyle}" Click="ExportExcelButton_Click"/>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <UniformGrid Grid.Row="0" Grid.ColumnSpan="2" Columns="4" Margin="0,0,0,14">
                <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="Total vânzări" FontSize="11" Foreground="{StaticResource MutedInkBrush}"/>
                        <TextBlock x:Name="TotalSalesText" Text="0 MDL" FontSize="22" FontWeight="Black" Foreground="{StaticResource PrimaryRedBrush}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="TVA total" FontSize="11" Foreground="{StaticResource MutedInkBrush}"/>
                        <TextBlock x:Name="TotalTvaText" Text="0 MDL" FontSize="22" FontWeight="Black"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" Margin="0,0,10,0">
                    <StackPanel>
                        <TextBlock Text="Comenzi" FontSize="11" Foreground="{StaticResource MutedInkBrush}"/>
                        <TextBlock x:Name="OrdersCountText" Text="0" FontSize="22" FontWeight="Black"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource KpiCardStyle}" Margin="0">
                    <StackPanel>
                        <TextBlock Text="Client top" FontSize="11" Foreground="{StaticResource MutedInkBrush}"/>
                        <TextBlock x:Name="TopClientText" Text="-" FontSize="15" FontWeight="Bold" Foreground="{StaticResource PrimaryRedBrush}" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </Border>
            </UniformGrid>

            <DataGrid Grid.Row="1" Grid.Column="0" x:Name="SalesGrid" AutoGenerateColumns="False"
                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                      Style="{StaticResource DefaultDataGridStyle}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Nr." Binding="{Binding IdComanda}" Width="70"/>
                    <DataGridTextColumn Header="Data" Binding="{Binding DataComanda, StringFormat=dd.MM.yyyy}" Width="105"/>
                    <DataGridTextColumn Header="Client" Binding="{Binding Client}" Width="*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="110"/>
                    <DataGridTextColumn Header="Valoare MDL" Binding="{Binding ValoareTotala, StringFormat=N2}" Width="130"/>
                    <DataGridTextColumn Header="TVA MDL" Binding="{Binding TVA, StringFormat=N2}" Width="110"/>
                    <DataGridTextColumn Header="Materiale" Binding="{Binding Materiale}" Width="220"/>
                    <DataGridTextColumn Header="Observații" Binding="{Binding Observatii}" Width="180"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="1" Grid.Column="1" Margin="14,0,0,0">
                <Border Style="{StaticResource CardBorderStyle}" Margin="0,0,0,14">
                    <StackPanel>
                        <TextBlock Text="Top clienți" FontSize="14" FontWeight="Bold" Foreground="{StaticResource PrimaryRedBrush}" Margin="0,0,0,8"/>
                        <ItemsControl x:Name="TopClientsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="95"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Name}" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Total, StringFormat=N2}" FontSize="11" FontWeight="Bold"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>

                <Border Style="{StaticResource CardBorderStyle}">
                    <StackPanel>
                        <TextBlock Text="Top materiale" FontSize="14" FontWeight="Bold" Foreground="{StaticResource PrimaryRedBrush}" Margin="0,0,0,8"/>
                        <ItemsControl x:Name="TopMaterialsList">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="0,5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="95"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding Name}" FontSize="11" TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Grid.Column="1" Text="{Binding Total, StringFormat=N2}" FontSize="11" FontWeight="Bold"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <StatusBar Grid.Row="3" Background="White" BorderBrush="{StaticResource LineBrush}" BorderThickness="1"
                   Padding="12,8" Margin="0,14,0,0">
            <StatusBarItem>
                <TextBlock x:Name="StatusText" Text="Gata" Foreground="{StaticResource MutedInkBrush}" FontSize="11"/>
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <TextBlock x:Name="RecordCountText" Text="Total: 0 comenzi" Foreground="{StaticResource MutedInkBrush}" FontSize="11"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>
