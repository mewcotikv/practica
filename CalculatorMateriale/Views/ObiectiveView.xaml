<UserControl x:Class="CalculatorMateriale.Views.ObiectiveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <DockPanel Background="{StaticResource CanvasBrush}" LastChildFill="True" Margin="24">
        <Border DockPanel.Dock="Top" Background="{StaticResource CanvasBrush}" Padding="0,0,0,18">
            <StackPanel>
                <TextBlock Text="Gestionare obiective" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Text="Obiective asociate clienților și suprafețelor de termoizolare."
                           Style="{StaticResource SubtitleTextBlockStyle}" FontSize="13"/>
            </StackPanel>
        </Border>

        <Border DockPanel.Dock="Top" Style="{StaticResource CardBorderStyle}" Margin="0,0,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="220"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="120"/>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="ClientCombo" DisplayMemberPath="Nume" SelectedValuePath="IdClient"
                          Height="36" Margin="0,0,10,0" Style="{StaticResource DefaultComboBoxStyle}"/>
                <TextBox x:Name="DenumireInput" Grid.Column="1" Height="36" Text="Obiectiv termoizolatie"
                         Style="{StaticResource DefaultTextBoxStyle}" Margin="0,0,10,0"/>
                <TextBox x:Name="SuprafataInput" Grid.Column="2" Height="36" Text="100"
                         Style="{StaticResource DefaultTextBoxStyle}" Margin="0,0,10,0"/>
                <TextBox x:Name="LocalitateInput" Grid.Column="3" Height="36" Text="Chișinău"
                         Style="{StaticResource DefaultTextBoxStyle}" Margin="0,0,10,0"/>
                <Button Grid.Column="4" Content="Adaugă" Style="{StaticResource PrimaryButtonStyle}"
                        Padding="10,7" Margin="0,0,10,0" Click="AddButton_Click"/>
                <Button Grid.Column="5" Content="Actualizează" Style="{StaticResource SecondaryButtonStyle}"
                        Padding="10,7" Margin="0,0,10,0" Click="UpdateButton_Click"/>
                <Button Grid.Column="6" Content="Șterge" Style="{StaticResource OutlineButtonStyle}"
                        Padding="10,7" Click="DeleteButton_Click"/>
            </Grid>
        </Border>

        <TextBlock x:Name="ValidationText" DockPanel.Dock="Top" Foreground="{StaticResource PrimaryRedBrush}" Margin="0,0,0,10"/>

        <DataGrid x:Name="ObiectiveGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                  Style="{StaticResource DefaultDataGridStyle}" IsReadOnly="True" SelectionChanged="ObiectiveGrid_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding IdObiectiv}" Width="60"/>
                <DataGridTextColumn Header="Denumire" Binding="{Binding Denumire}" Width="*"/>
                <DataGridTextColumn Header="Client" Binding="{Binding Client.Nume}" Width="180"/>
                <DataGridTextColumn Header="Suprafață m²" Binding="{Binding SuprafataM2}" Width="120"/>
                <DataGridTextColumn Header="Localitate" Binding="{Binding Localitate}" Width="140"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Creat" Binding="{Binding DataCrearii, StringFormat=dd.MM.yyyy}" Width="110"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</UserControl>
